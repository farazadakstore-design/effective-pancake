# تطبيق تقرير الأقسام — مشروع أندرويد جاهز (Capacitor)

تم تحويل ملف الويب `تقرقر القسم.html` إلى مشروع أندرويد كامل باستخدام Capacitor. الملف داخل `www/index.html` هو نفس التطبيق (تسجيل الدخول، الفنيون، التقرير، التقييم، الإعدادات) بدون أي تعديل في الوظائف.

## لماذا لم أُرفق ملف APK جاهزًا؟

بيئة العمل التي أُنشئ فيها هذا المشروع محجوبة عن الوصول لخوادم Google Maven و Maven Central و Gradle (المطلوبة لتنزيل مكتبات أندرويد أثناء البناء)، لذلك لا يمكنني تجميع ملف APK فعليًا من هنا. لكن المشروع المرفق **جاهز 100% للبناء** على جهازك، والخطوة الوحيدة الناقصة هي تشغيل أمر بناء واحد على جهاز فيه إنترنت طبيعي.

## طريقة الحصول على APK عبر GitHub (بدون تثبيت أي برنامج) — موصى بها

المشروع فيه ملف جاهز `.github/workflows/build-apk.yml` يخلي خوادم GitHub (عندها إنترنت كامل، غير محجوبة) تبني ملف الـ APK تلقائيًا كل ما ترفع الكود. الخطوات:

1. سوّ حساب مجاني على https://github.com إذا ما عندك.
2. اضغط **New repository** (زر أخضر أعلى يمين الصفحة الرئيسية) → اكتب اسم مثل `dept-report-app` → خليه **Private** أو Public حسب رغبتك → **Create repository**.
3. في صفحة الريبو الفاضية، اضغط رابط **uploading an existing file**.
4. فك ضغط ملف `dept-app-android.zip` على جهازك، ثم اسحب **كل محتويات** مجلد `dept-app-android` (بما فيها مجلد `.github` و `android` و `www`) وأفلتها في صفحة الرفع. **لا ترفع مجلد `node_modules` إذا كان موجود** (احذفه قبل الرفع، أصلاً غير مطلوب).
5. اكتب أي رسالة Commit بالأسفل واضغط **Commit changes**.
6. روح لتبويب **Actions** أعلى الصفحة — بتلاقي مهمة "Build Android APK" شغالة تلقائيًا (تاخذ 3-5 دقائق أول مرة).
7. لما تخلص (علامة ✅ خضراء)، افتحها → بالأسفل قسم **Artifacts** → نزّل `app-debug-apk` (يجي كملف zip فيه الـ APK بداخله).
8. فك الضغط، انسخ `app-debug.apk` لهاتفك وثبّته.

بعدين أي تعديل ترفعه على GitHub، البناء يصير تلقائيًا من جديد ويطلع لك APK جديد من نفس المكان.

**بديل لمن يعرف Git:**
```
cd dept-app-android
git init
git add .
git commit -m "أول نسخة"
git branch -M main
git remote add origin https://github.com/USERNAME/dept-report-app.git
git push -u origin main
```
(بدّل `USERNAME` باسم حسابك، و`dept-report-app` باسم الريبو اللي سويته)

## طريقة الحصول على APK (Android Studio)

1. نزّل ثبّت **Android Studio** (مجاني): https://developer.android.com/studio
2. افتح Android Studio → Open → اختر مجلد `android` الموجود داخل هذا المشروع.
3. انتظر حتى ينتهي "Gradle Sync" تلقائيًا (يحتاج إنترنت، يحصل مرة واحدة فقط).
4. من القائمة: **Build → Build Bundle(s) / APK(s) → Build APK(s)**.
5. بعد الانتهاء تظهر رسالة فيها رابط "locate" يفتح مكان ملف الـ APK (عادة داخل `android/app/build/outputs/apk/debug/app-debug.apk`).
6. انسخ الملف لهاتفك وثبّته (فعّل "السماح بتثبيت من مصادر غير معروفة" إذا طلب ذلك).

## طريقة بديلة (سطر الأوامر)

إذا عندك Java 17 و Android SDK مثبّتين على جهازك:

```
cd android
./gradlew assembleDebug
```

الملف الناتج: `android/app/build/outputs/apk/debug/app-debug.apk`

لإصدار نسخة موقّعة للنشر (Release) استخدم `./gradlew assembleRelease` مع إعداد مفتاح توقيع (keystore) — Android Studio يوفر معالج جاهز لذلك من Build → Generate Signed Bundle / APK.

## ملاحظات مهمة

- بيانات التطبيق (الفنيون، التقارير، التقييمات) تُحفظ محليًا على كل جهاز (localStorage) — نفس آلية النسخة الأصلية على المتصفح، لا يوجد خادم مركزي.
- التطبيق يحتاج اتصال إنترنت لتحميل مكتبتي إنشاء PDF (jsPDF, html2canvas) من الشبكة عند إرسال التقارير عبر واتساب، ولفتح روابط واتساب نفسها.
- اسم الحزمة (Package ID): `com.factory.deptreports` — يمكن تغييره من `android/app/build.gradle` و `capacitor.config.json` قبل البناء إذا رغبت.
- أيقونة التطبيق حاليًا هي الأيقونة الافتراضية لـ Capacitor. لتغييرها: انقر بالزر الأيمن على `android/app` في Android Studio → New → Image Asset، وارفع شعارك.
- إذا عدّلت الملف `www/index.html` لاحقًا وأردت مزامنته مع مشروع أندرويد، شغّل داخل هذا المجلد:
  ```
  npm install
  npx cap sync android
  ```
