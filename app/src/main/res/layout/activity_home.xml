<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <include layout="@layout/view_header" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Sections card -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/bg_card"
                android:padding="16dp"
                android:layout_marginBottom="14dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/home_title"
                    android:textColor="@color/text_main"
                    android:textSize="19sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/home_sub"
                    android:textColor="@color/muted"
                    android:textSize="13sp"
                    android:layout_marginBottom="8dp" />

                <GridLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:columnCount="2"
                    android:rowCount="3"
                    android:alignmentMode="alignMargins"
                    android:useDefaultMargins="false">

                    <LinearLayout android:id="@+id/btnKhayat" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="🧵" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_khayat" />
                    </LinearLayout>

                    <LinearLayout android:id="@+id/btnNajjar" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="🪚" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_najjar" />
                    </LinearLayout>

                    <LinearLayout android:id="@+id/btnAbwab" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="🚪" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_abwab" />
                    </LinearLayout>

                    <LinearLayout android:id="@+id/btnAluminum" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="🪟" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_aluminum" />
                    </LinearLayout>

                    <LinearLayout android:id="@+id/btnKahraba" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="⚡" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_kahraba" />
                    </LinearLayout>

                    <LinearLayout android:id="@+id/btnMuno" style="@style/SectionButton">
                        <TextView style="@style/SectionIcon" android:text="🧰" />
                        <TextView style="@style/SectionLabel" android:text="@string/sec_muno" />
                    </LinearLayout>
                </GridLayout>
            </LinearLayout>

            <!-- Recent reports card -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/bg_card"
                android:padding="16dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/recent_title"
                    android:textColor="@color/text_main"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="8dp" />

                <LinearLayout
                    android:id="@+id/recentContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />
            </LinearLayout>
        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
